let alturaCm = 170;

let alturaM = 1.7;

let peso = 67.5;

let alturaRound = alturaM.toFixed(0);

console.log(alturaRound);

let pesoRound = parseInt(peso);

console.log(pesoRound);

let max_valor_JS = Number.MAX_VALUE;

let comparacion = max_valor_JS + 1 === max_valor_JS;

console.log(comparacion);
